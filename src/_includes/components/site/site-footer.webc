<footer webc:root="override">
  <p>
    &copy; <template webc:nokeep @raw="$data.site.copyrightYear"></template>
    <template @raw="$data.site.author.name" webc:nokeep></template>
  </p>

  <site-navigation></site-navigation>
</footer>

<style webc:scoped>
  :host {
    display: flex;
    flex-direction: column;
    gap: var(--step-0);
    text-align: center;
    font-size: var(--step--1);
    padding: var(--step-0);
  }

  p {
    word-break: keep-all;
  }

  a {
    font-weight: inherit;
    text-decoration-thickness: inherit;
    text-decoration-color: inherit;
  }

  /* override navigation styles */
  nav > ul {
    justify-content: center;
  }

  @media (min-width: 28rem) {
    :host {
      flex-direction: row;
      justify-content: space-between;
    }
  }
</style>
